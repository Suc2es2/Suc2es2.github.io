---
title: 2020 "之江杯" 工业互联网内生安全防御国际精英挑战赛 WriteUp
date: 2020-04-14 13:18:00
categories: 
  - CTF
tags:
  - CTF
---

# 2020 "之江杯" 工业互联网内生安全防御国际精英挑战赛 WriteUp

- 主办与支持：比赛由之江实验室联合中国信通院等机构共同举办，并获得了中国工程院和浙江省人民政府的支持

- 赛事概况：比赛于 2020 年 12 月 20 日至 22 日举办，为期三天。共邀请了来自中、美、俄、德等 8 个国家的 40 支顶尖 "白帽黑客" 战队参赛。赛事的核心目标是攻击之江实验室研发的工业互联网拟态防御系列设备

- 赛制与创新：大赛设置了 CTF 竞速赛、AWD 竞技赛、人机对抗赛和巅峰挑战赛四个环节。最大亮点是首次在工业互联网安全领域引入 "白盒" 人机对抗模式，即向参赛队伍开放部分系统控制权，允许其设置后门或植入病毒，以极端方式测试系统的防御能力

- 比赛结果：经过 54 小时、高达 95 万次的高强度攻击，没有任何一支队伍能够成功突破由拟态防御技术构筑的防线。这一结果验证了我国独创的内生安全理论与拟态防御技术在工业互联网领域的有效性

## Misc

### 简单 Modbus 协议分析（Modbus 协议）

![](https://pic1.imgdb.cn/item/6a320d7fb67c7e4f4a887fa7.png)

502 是 Modbus/TCP 常用端口

![](https://pic1.imgdb.cn/item/6a320b36b67c7e4f4a887510.png)

正常看了一圈找不到明显异常，没有别的功能码

正常情况下，以太网帧长度大致应该是：

```
Ethernet header 14 字节 + IP total length
```

下面过滤语法可以筛选出存在额外字节的流量包

```
tcp.port == 502 && frame.len > ip.len + 14
```

- `frame.len`：整个数据帧在物理网络上的总长度（从以太网头部开始，到数据包末尾结束）

- `ip.len`：IP 数据包的总长度（仅包含 IP 头部 + TCP 头部 + 应用层数据，不包含以太网链路层头部）

- `+ 14`：标准的 以太网 II 帧头部长度（6 字节目标 MAC + 6 字节源 MAC + 2 字节类型/长度字段）

![](https://pic1.imgdb.cn/item/6a320d29b67c7e4f4a887607.png)

转为 ASCII 码得到 `flag{DGswTfgy1GD236fs2sfF2dskLng}`

### 工控现场的恶意扫描（Modbus 协议）

![](https://pic1.imgdb.cn/item/6a323c9881fd5b073795679c.png)

题目说 PLC 被扫描，PLC 常见协议优先看：

- Modbus/TCP -> 502 端口

- S7comm     -> 102 端口

- EtherNet/IP -> 44818 端口

这份包里最明显的是 502 端口，也就是 Modbus/TCP

可以看到有两个主机在访问 PLC

![](https://pic1.imgdb.cn/item/6a323a2981fd5b07379561aa.png)

按照长度排列找到 flag

```
ip.src == 192.168.99.32 && ip.dst == 192.168.99.31 && tcp.dstport == 502
```

![](https://pic1.imgdb.cn/item/6a323bdb81fd5b0737956514.png)

### 注册表分析（.reg 注册表取证）

![](https://pic1.imgdb.cn/item/6a323ca781fd5b07379567a8.png)

注册表文件是 UTF-16LE 的 `.reg` 文本，直接搜索无线网络相关字段

```
grep -a "NetworkList" zhucebiao.reg
grep -a "Wlansvc" zhucebiao.reg
grep -a "ProfileName" zhucebiao.reg
```

关键位置在 `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\Profiles`

可以看到两个网络配置：

```
"ProfileName"="OPPO Reno"
"Description"="OPPO Reno"

"ProfileName"="网络"
"Description"="网络"
```

但真正的无线网络在 `HKEY_LOCAL_MACHINE\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\Nla\Wireless`

里面有

```
@="4F50504F2052656E6F"
```

解码是 `OPPO Reno`，也就是黑客连接过的 WiFi 名称

另外还能看到对应网关 MAC

```
DefaultGatewayMac=hex:6e,58,6e,70,69,44
```

### modbus

### S7协议恶意攻击分析

### 上位机通讯异常分析

### 梯形图2

### S7Comm 攻击协议分析